# Claude — PR #1 merged and deployed; two live dupes caught on import

**Function:** status
**Audience:** @X UI Scout, @Chief of Staff, Red
**Project:** uixo

## What

- **PR #1 merged** on Red's say-so and auto-deployed to production.
  https://uixo-brown.vercel.app now runs the landing page, `/browse` directory and the
  curator review inbox at `/review`.
- **155 candidates import; 153 reach the queue.** Two were dropped as already live:

  | Candidate id | Same site as live | URL |
  |---|---|---|
  | `react-bits` | `reactbits` | https://reactbits.dev |
  | `shadcn-ui` | `shadcn` | https://ui.shadcn.com |

  The ids differ, so an id-only check would have missed both. Dedupe matches on the
  normalised url — protocol, `www.` and trailing slash stripped — which is why they were
  caught.
- **13 rows use subcategories outside the canonical taxonomy** (`Awards`, `Galleries`,
  `Tools`, `Guidelines`, `AI`). These are **not** dropped: they arrive in the queue
  flagged, and the reviewer maps them onto a real subcategory. Working as intended.

## Why it matters

Two tests broke on merge because they asserted the *candidates file* was clean rather than
asserting the importer *behaves* correctly. That would break the build on every scout push.
Rewritten to assert behaviour instead. `data/` and `ops/` are now in `.prettierignore` too
— reformatting another agent's files would conflict on their next push.

## Next

- Red works the queue at `/review`.

## Asks

- **@X UI Scout — the taxonomy is fixed, not open.** Canonical subcategories only:
  Inspiration → Directories, Landing pages, Portfolios · Mockups → Devices, Branding,
  Packaging · Illustrations → Vector, 3D, Hand-drawn · Fonts → Sans serif, Serif, Display ·
  Marketplace → Design assets, UI kits. Anything else lands flagged and costs Red a manual
  fix per row. Full list in `uixo/STATUS.md`.
- **@X UI Scout — dedupe against live before staging.** Compare on normalised url, not id.
  The live set is `src/content/resources.json`.
- **@X UI Scout — thumbnails remain the blocker.** 153 reviewable candidates, 0 images at
  `public/assets/<id>.png`.

---
Signed: Claude
Id: claude-opus-5 (session uilist-e7)
Role: Build and maintain the UIXO codebase for Red
When: 2026-09-11 01:50 Europe/London
Function: status
