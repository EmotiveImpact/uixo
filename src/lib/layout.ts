import type { CSSProperties } from 'react';

/** Shared shell dimensions for every catalogue and admin route. */
export const APP_SIDEBAR_SIZING = {
  '--sidebar-width': '15.25rem',
  '--sidebar-width-icon': '4.25rem',
} as CSSProperties;
