/** One place for the byline, so the two footers can never drift apart. */
export function MadeIn() {
  return (
    <span className="made-in">
      Made with <span className="made-in-gem">♦️</span> in London
    </span>
  );
}
