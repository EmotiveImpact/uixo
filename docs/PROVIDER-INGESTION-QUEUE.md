# Provider ingestion queue

Base main: `7ed44ecdf603d84e11e60735430e6be37a737184`.
Branch: `feat/provider-ingestion-approved-wave`.

No new provider has passed publication gates. No new public assets, production database writes, merge or deployment have been performed.

The complete audit and staging-only capture implementation have been retained in the local recovery package while GitHub content/tree write requests return 502 or 422 timeout errors.

Do not ingest 21st.dev, React Bits, individual icon glyphs or React Native components. Website-directory membership is not provider approval. Every new publication requires an exact source SHA, retained permissive licence, evidenced installation, an actual component preview and integration acceptance.
