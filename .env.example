# Neon Postgres connection string. Use the pooled endpoint for serverless functions.
# Copy to .env.local and fill in — .env* is gitignored.
DATABASE_URL=postgresql://user:password@ep-xxx-pooler.region.aws.neon.tech/uixo?sslmode=require

# Direct (unpooled) endpoint, for migrations.
DATABASE_URL_UNPOOLED=postgresql://user:password@ep-xxx.region.aws.neon.tech/uixo?sslmode=require

# Absolute origin used for canonical URLs, Open Graph tags, the sitemap and the feed at
# build time. It must be a host the site is actually reachable at: pointing canonicals at a
# domain you do not control hands your ranking to whoever does own it.
# Until uixo.io is attached in Vercel, production sets this to the vercel.app alias.
SITE_URL=https://uixo.io

# Neon Auth (managed Better Auth). Vite and the Vercel /api/auth function proxy this
# through the site origin so the session cookie is first-party.
NEON_AUTH_BASE_URL=https://ep-xxx.neonauth.region.aws.neon.tech/neondb/auth

# Signs the local session_data cookie minted by the auth proxy. Generate with
# `openssl rand -base64 32`. Required in production; a local fallback exists.
NEON_AUTH_COOKIE_SECRET=

# Set to "true" only to preview a production build locally while auth is still mocked.
# Leaving this unset makes a production bundle refuse to start with the mock provider.
VITE_ALLOW_MOCK_AUTH=
