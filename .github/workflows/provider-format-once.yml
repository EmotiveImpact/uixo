name: Format ingestion metadata once

on:
  push:
    branches:
      - feat/provider-ingestion-approved-wave
    paths:
      - '.github/workflows/provider-format-once.yml'

permissions:
  contents: write

jobs:
  format:
    if: github.repository == 'EmotiveImpact/uixo'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.sha }}
          persist-credentials: true
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - run: npm ci --ignore-scripts
      - run: npx prettier --write docs/PROVIDER-INGESTION-QUEUE.md .github/workflows/provider-ingestion-capture.yml
      - name: Commit formatting only
        run: |
          git add -- docs/PROVIDER-INGESTION-QUEUE.md .github/workflows/provider-ingestion-capture.yml
          if git diff --cached --quiet; then exit 0; fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'style: format provider ingestion metadata'
          git push origin 'HEAD:refs/heads/feat/provider-ingestion-approved-wave'
